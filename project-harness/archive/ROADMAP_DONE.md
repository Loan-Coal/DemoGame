# ROADMAP — Archived Phases (C++ Complete)

> Phases moved here have their C++ implementation committed and `Scripts/check.ps1` green.
> Remaining editor / art / live-test work for these phases is tracked in
> `project-harness/UNREAL_EDITOR_PLAYBOOK.md` and `project-harness/HUMAN_VERIFICATION.md`.
> **Do not edit this file** — it is a C++ history record, not a task list.

---

## Bring-up — before any new game work

- [x] **Generate project files + full build.** *(Done 2026-06-23: fixed dynamic-delegate binding in `DialogueWidgetBase` — `AddUObject`/`Remove` → `AddDynamic`/`RemoveDynamic` + `UFUNCTION()` handlers. Both module DLLs build clean.)*
- [x] **Create `Config/NpcEngine.ini`** from template. *(2026-06-23: default localhost:8000 + dev key; gitignored.)*
- [x] **Bring up the Docker engine and prove the middleware end-to-end.** *(2026-06-23: verified live — Health OK, Mira reply "Good evening to you as well…", DegradationLevel=full, Cached/Fallback=false, PASS. Fixed config bug: GConfig truncated `http://…` at `//`; now reads the ini manually.)*
- [x] **Register `NpcEngine.Smoke` + write ≥1 client `*.spec.cpp`.** *(2026-06-23: `NpcEngineSmokeCommand.cpp`; `NpcEngineJsonUtils.spec.cpp` = 8 specs, all pass. Fixes ISSUE-001.)*

---

## Phase 0 — Engine Smoke Test

**Goal:** Verify the NPC Engine is reachable and seed data is correct before writing any Unreal code.
**Status:** ✅ C++ complete.

- [x] Run `make demo-seed` / verify player_demo and mira_innkeeper nodes via GET /v1/graph/nodes
- [x] PowerShell smoke test: Health 200 + non-empty `npc_response`
- [x] Confirm `npc_response` is non-empty; record as baseline
- [x] Store NPC_ENGINE_URL + NPC_ENGINE_API_KEY in gitignored `Config/NpcEngine.ini`

---

## Phase 1 — NpcEngineClient (TDD)

**Goal:** The sole C++ network layer; gameplay receives typed USTRUCTs, never raw JSON.
**Status:** ✅ C++ complete. All Automation Specs green.

- [x] Module skeleton: `NpcEngineClient.h/.cpp/.Build.cs`
- [x] File headers on every `.h`/`.cpp`
- [x] Failing Automation Specs first (`NpcEngineClientSpec.cpp`):
  - [x] `GetHealth()` parses OkEnvelope → `FHealthResponse`
  - [x] `PostDialogue(FDialogueRequest)` → populated `FDialogueResponse`
  - [x] Empty `player_message` → `ERequestError::InvalidInput` (no HTTP call)
  - [x] `player_message` > 1000 chars → `ERequestError::InvalidInput`
  - [x] Non-2xx → `ERequestError::NetworkError`; fallback text set
  - [x] `PostClockAdvance(int32)` → calls `/clock/advance`, returns bool
  - [x] `PostAction(FActionReportRequest)` → calls `/v1/action`, returns raw status
  - [x] `UpsertNode`, `UpsertEdge`, `CheckNodeExists` — each with spec
- [x] `UNpcEngineRestClient` implemented:
  - [x] Reads URL + API key from ini/env; Fatal if missing
  - [x] All HTTP via `FHttpModule`; callbacks on game thread
  - [x] Response shape branch: raw body for `/v1/dialogue` + `/v1/action`; OkEnvelope for others
  - [x] `FDialogueResponse` USTRUCT: all openapi.json fields
  - [x] `EFacialExpressionType` UENUM: Neutral/Happy/Wary/Fearful/Angry/Sad; unknown → Neutral + log
  - [x] `EActionType` UENUM: Speak default; unknown → coerce + log
  - [x] Dialogue timeout 30 s; Health timeout 3 s; both configurable via ini
  - [x] Fallback: `DA_NpcFallbackLines` DataAsset; used on timeout/non-2xx
- [x] `INpcDialogueService` UInterface: pure-virtual `SendDialogue`, `SendActionReport`, `GetNpcState`
- [x] `UNpcEngineServiceSubsystem` (GameInstance): owns one `UNpcEngineRestClient`; exposes service interface; `SetDialogueService()` for test injection
- [x] `FNpcEngineWebSocketClient` scaffold: delegates `OnDialogueChunk`/`OnDialogueComplete`; parse token/done/error/proactive_line; all `// TODO(SLICE-2-WS)` — not wired to gameplay
- [x] `NpcEngine.Smoke` exec command: calls GetHealth + one PostDialogue; logs npc_response
- [x] Gameplay skeleton (DemoGame module, Net I/O: no on all files):
  - [x] `ANpcActorBase`: `FName NpcId`; `USphereComponent` proximity; interact prompt
  - [x] `ADemoGameCharacter`: finds nearest NpcActorBase on E/IA_Interact; BeginDialogue; spawns WBP_Dialogue; restores input on end
  - [x] `UDialogueManagerSubsystem`: resolves service lazily; `SetDialogueService()` seam; routes SubmitMessage
  - [x] C++ UMG base widget: rejects empty/oversized input; BlueprintNativeEvent display handlers with C++ defaults
- [x] DIP complete (DEC-013): 4 `UDialogueManager` Automation Specs using fake `INpcDialogueService`; zero DemoGame refs to `UNpcEngineRestClient`
- [x] All Automation Specs green. No file > 500 lines.
- [x] `docs/STORYBOARD.md` authored
- [x] USTRUCTs annotated with `meta=(JsonName="...")` — `check_contract_sync.py` now enforcing PASS

---

## Phase 2 — World Skeleton + Tick

**Goal:** Three navigable locations; location arrival fires a world clock tick.
**Status:** ✅ C++ complete 2026-06-24. Procedural greybox complete 2026-06-25.

- [x] `/clock/advance` shape confirmed per DEC-014
- [x] `AdvanceClock(int32 DeltaTicks)` added to `INpcDialogueService`; `ClockAdvance.spec.cpp` written first
- [x] `ANpcLocation` Actor: `FName LocationId`, `bool bFiresTick`, `USphereComponent TriggerVolume` → `UNpcWorldSubsystem::OnPlayerArrived`
- [x] `UNpcWorldSubsystem` (World Subsystem): re-entry guard, `AdvanceClock(1)`, `OnTickAdvanced.Broadcast()`; 5 Automation Specs
- [x] `UArrivalSubtitleWidget`: binds `OnTickAdvanced`; `FadeOutAnimation` (BindWidgetAnimOptional); `DA_LocationSubtitles` DataAsset
- [x] `UGreyboxWorldSubsystem`: 4 canonical locations (loc_tavern, loc_tavern_back, loc_market_square, loc_guard_barracks); `EnsureBuilt()` idempotent; `GetLocationCenter(FName)`. Automation Spec: layout + NPC roster ↔ layout consistency.
- [x] `UNpcSpawnerSubsystem` wired to layout: NPCs placed at `GetLocationCenter(LocationId)`

**Human tail:** PIE smoke test (greybox world + live-engine tick logging). See HUMAN_VERIFICATION.md.

---

## Phase 3 — Seeder + Seed Data

**Goal:** DemoGame owns all world seeding; idempotent against a fresh engine.
**Status:** ✅ C++ complete 2026-06-24.

- [x] Tavern name "The Broken Flagon" (DEC-017); town "Thornfield" throughout
- [x] Failing Automation Specs for `UNpcWorldSeeder`: idempotency, dependency order, halt on non-2xx, non-blocking
- [x] `Seed/DemoWorld_v1.json` authored — Locations, Characters (all 5 NPCs + player_demo), Factions, Event (northern_war_begins), Edges, Quests
- [x] `UNpcWorldSeeder`: reads DemoWorld_v1.json; CheckNodeExists → skip/upsert; seed order: Locations → Factions → Characters → Events → Edges → Quests; halt on non-2xx
- [x] `NpcEngine.SeedWorld` exec command (FAutoConsoleCommandWithWorld; NpcEngineClient module)
- [x] `MemoryBadgeDefaults.h` — C++ stub map for all 20 seed memory node IDs
- [x] `DemoWorld_v1.json` reconciled with ENGINE_CONTRACT.md

---

## Phase 4 — Dialogue + Trust Gate (Greybox)

**Goal:** Complete investigation step — approach Mira, build trust, cross trust gate 1.
**Status:** ✅ C++ complete 2026-06-24.

- [x] Failing functional Automation Spec for `UDialogueComponent`: empty/oversized input rejected client-side; fallback fires on non-2xx; LSP enforced
- [x] `UDialogueComponent` on `ANpcActorBase`: StartDialogue, SubmitMessage, trust/session/fallback handling; OnFallbackLine, OnTrustChanged, OnMemoriesRecalled, OnFacialExpression
- [x] `UDialogueWidgetBase`: input locked during wait; `ResponseText` history; `ThinkingIndicator`; `MemoriesBadge` (5 s timer); BindWidgetOptional for all slots
- [x] `URelationshipMeterWidget`: trust/fear/affection signed-integer display; `OnRelationshipUpdated` BlueprintNativeEvent

**Human tail:** Live trust-gate verification (3 runs from fresh seed; Mira must reveal Aldric info above trust 25). See HUMAN_VERIFICATION.md.

---

## Phase 5 — Quest System + Faction Fork (Greybox)

**Goal:** Full chain A playable — investigate → identify Aldric → deliver/sell → quest log complete.
**Status:** ✅ C++ complete 2026-06-24.

- [x] DEC-025/026/027/028 added to DECISIONS.md
- [x] `INpcQuestService` UInterface (ISP split per DEC-026): QuestOffer, QuestAccept, QuestObjective, QuestEvaluate, QuestReward, QuestChoose
- [x] Quest lifecycle specs (NpcEngineClient/Tests) — all 6 calls verified; non-2xx logged + returns false
- [x] Quest lifecycle implemented in `UNpcEngineRestClient`
- [x] `UQuestSubsystem`: ActivateQuest, CompleteStep, quest data as C++ map (DEC-028)
- [x] `UQuestLogWidget` C++ base: active quest list; steps with completion checkboxes
- [x] `UQuestConfirmWidget` C++ base: QuestTitleText, AcceptButton, DeclineButton
- [x] `UNpcFactionSubsystem`: TMap<FName, int32> standings; updated from action response relation_deltas
- [x] `UFactionForkWidget` C++ base: ChoiceAButton/ChoiceBButton/ChoiceALabel/ChoiceBLabel
- [x] `L_TavernBack` streaming gate: trust ≥ TRUST_GATE_2_MIRA (40) → OnTavernBackUnlocked delegate
- [x] `UNpcSaveGame`: PlayerId, quest step states, faction standings; load → CheckNodeExists → re-seed edge case

**Human tail:** Chain A end-to-end PIE test; streaming gate PIE test. See HUMAN_VERIFICATION.md.

---

## Phase 6 — Gossip Chain + Rumor Journal (Greybox)

**Goal:** Distorted rumor at Old Henryk; Rumor Journal shows full chain; Sorn quest unlocks.
**Status:** ✅ C++ complete 2026-06-24.

- [x] `FNpcStateSnapshot` USTRUCT + spec (parses `GET /v1/npc/{npc_id}/state` OkEnvelope .data)
- [x] `GetNpcState` callback → `FNpcStateSnapshot` (DEC-029)
- [x] `UGossipCacheSubsystem` specs: AddGossipEntry, GetChainForEvent, MarkPlayerSpokeToNpc gate
- [x] `UGossipCacheSubsystem`: after tick calls GetNpcState(OldHenryk); `learned_facts` updates Henryk entry; `MarkPlayerSpokeToNpc` gating
- [x] `URumorJournalWidget` C++ base: reads GossipCacheSubsystem on Tab; `OnJournalRefreshed` BlueprintNativeEvent; `GetDistortionLabel(HopCount)` → Firsthand/Rumor/Distorted; bPlayerSpokeToNpc gate; ChainScrollBox/ChainContainer BindWidgetOptional
- [x] `ANoticeBoard`: RumorTiers + TierTickThresholds UPROPERTYs; listens to OnTickAdvanced; GetCurrentTierText()
- [x] Sorn quest gate: trust ≥ TrustGateSornQuest (50) → QuestSubsystem::ActivateQuest(patrol_duty)

**Human tail:** Gossip chain 5/5 gate; greybox acceptance gate (all 17 beats). See HUMAN_VERIFICATION.md.

---

## Phase 7 — MetaHuman + Dialogue Camera

**Goal:** MetaHuman actors; cinematic camera; engine-driven expressions.
**Status:** ✅ C++ complete 2026-06-25.

- [x] `NpcAppearance.h/.cpp`: `GetAvatarClass(FName NpcId)` backed by empty TMap by default (cube stand-in); one-row-per-NPC to swap. `NpcAppearance.spec.cpp`: 3 specs.
- [x] `FNpcSpawnRecord` gains `AvatarClass`; spawner fills from `GetAvatarClass(NpcId)`
- [x] `ANpcGreyboxActor::TrySpawnAvatar()`: if AvatarClass set → load + spawn child at feet → hide cube + label
- [x] `UFacialExpressionMapper` (NpcEngineClient): `ApplyExpression(USkeletalMeshComponent*, EFacialExpressionType, int32)` → sets morph target weight; C++ default map ships placeholder ARKit names; null mesh → warning; unknown → Neutral. 3 specs.
- [x] `UDialogueComponent` camera blend: OnStartDialogue → SetViewTargetWithBlend(NpcOwner, 0.3f, Cubic); OnDialogueClose → blend back to player 0.4 s
- [x] `ADemoGameCharacter` tracks `ActiveNpc`; calls `EndDialogue(PC)` in HandleDialogueEnded

**Human tail (Phase 11 + Phase 7 Editor Session):** MetaHuman import, DialogueCamSocket socket setup, AnimBP expression wiring. See UNREAL_EDITOR_PLAYBOOK.md §1–§2.

---

## Phase 8 — Environment Art Pass

**Goal:** Replace box geometry with Fab/Megascans environment assets.
**Status:** ✅ No C++ required. Fully editor work.

**Editor work:** See UNREAL_EDITOR_PLAYBOOK.md §4 (Levels/Environment) for the complete step-by-step.

Key decisions: baked static lightmaps (no Lumen in demo v1, 12 GB VRAM budget); NavMeshBoundsVolumes in all 4 levels; L_TavernBack streaming perf gate.

---

## Phase 9 — UI Polish + Audio

**Goal:** Dialogue panel art, Rumor Journal art, relationship meter HUD, audio — shipped quality.
**Status:** ✅ No C++ required. Fully editor work.

**Editor work:** See UNREAL_EDITOR_PLAYBOOK.md §3 (UI/UMG) and §5 (Audio) for the complete step-by-step.

---

## Phase 10 — Content Polish + Acceptance Validation

**Goal:** All authored content finalized; acceptance criteria passed.
**Status:** ✅ Headless authoring complete 2026-06-25. Live-engine gates and playtest pending.

- [x] All notice board texts authored: 3 tiers × all boards; Thornfield in all text. `MakeNoticeTiers()` in `NpcSpawnerSubsystem.cpp`.
- [x] Full `memories_recalled` badge lookup table: all 20 node IDs in `MemoryBadgeDefaults.h` replaced with final authored text; raw IDs never shown to player.
- [x] Fallback canned lines reviewed and finalized for all 5 NPCs in `NpcFallbackDefaults.h`.

**Live-engine human tails:** Aldric memory seeding, gossip chain 5/5 final run, distortion review, DemoWorld_v1.json reconciliation, naive user playtest, acceptance checklist. See HUMAN_VERIFICATION.md.

---

## Phase 11 — Asset Acquisition (Parallel Track)

**Goal:** All visual and audio assets acquired and project-ready before Phases 7–9 begin.
**Status:** ✅ Shortlist complete 2026-06-25 (CONDITIONAL GO verdict). Human editor work required.

**Editor work:** See UNREAL_EDITOR_PLAYBOOK.md §1 (MetaHuman creation and VRAM gate) for the complete step-by-step.

Shortlist: `project-harness/asset-shortlists/metahuman-clothing.md`
Clothing: see `project-harness/appendix-clothing-blender.md` for the optional free-CC-BY path.
